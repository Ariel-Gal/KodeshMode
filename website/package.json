{
  "name": "kodeshmode-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "build:vite": "vite build",
    "preview": "vite preview",
    "check:security": "node scripts/security-check.mjs",
    "check": "npm run build && npm run check:security"
  },
  "devDependencies": {
    "vite": "8.0.16"
  }
}
